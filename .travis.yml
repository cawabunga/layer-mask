language: node_js

node_js:
  - "8"

install:
  - "npm install"

addons:
  chrome: stable

before_install:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

after_success: 'npm run coveralls'